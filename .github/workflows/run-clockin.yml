name: Go

on:
  schedule:
    - cron:  '50 0 * * *'

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.17

    - name: Run
      run: go run In/main.go
      env:
        USERNAME: ${{ secrets.USERNAME }}
        PASSWORD: ${{ secrets.PASSWORD }}
        LAT: ${{ secrets.LAT }}
        LONG: ${{ secrets.LONG }}
      
